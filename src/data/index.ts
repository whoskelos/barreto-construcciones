import data from './data.json';

// Exportar con tipos para mejor autocompletado
export const { menu, services, projects, contact, social, legal, contactPage } = data;

// Exportar también el objeto completo por compatibilidad
export default data;
